@model IEnumerable<Sessao>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Horas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @item.Horas.ToString(@"hh\:mm")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>
                    <a asp-action="EditarSessao" asp-route-id="@item.Id"
                    data-ajax="true" data-ajax-method="get" 
                    data-ajax-mode="replace" data-ajax-update="#SessaoForm">Editar</a>
                </td>
            </tr>
    }
    </tbody>
</table>